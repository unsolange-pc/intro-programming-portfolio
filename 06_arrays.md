array: data structure that stores multiple values of the same type 
array = a list of values stored together.
array used to store multiple values of the same type in one variable.
<br>
## 2. Key C# Syntax
<br>
int[] A = { 15, 20, 25, 30, 35};

Console.WriteLine(numbers[0]); // first element

for (int i = 0; i < numbers.Length; i++)
{
    Console.WriteLine(numbers[i]); // loop through array
}
<br>
int[] = array of integers
numbers[0] = index access ("first element")
Length = size of array =5
<br>
## 3. Eureka Exercise / Moment
<br>
## 4. Mistakes
 i didn't understand the concept of array what
needs to be done and how to store multiple values an array
<br>

From notes and videos.
