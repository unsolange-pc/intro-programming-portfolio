# Topic Name
## 1. Concept in My Own Words
[
Arithmetic Operators: Addition(x+n), Subtraction(x-n), Multiplication(x*n), Division(x/n) , Modulo(remainder) x%n  
The arithmetic expression will result 
in/evaluate to a number (or char).
Relational/Comparison Operators Simple Boolean Expression : These types of expressions are 
called simple boolean expression. They consist of a relational operator 
surrounded by two simple operands, as illustrated above. They result in/evaluate to a Boolean value:  true or false

Conditional Logic Operators Complex Boolean Expression :
&& takes one False to evaluate to F 
|| takes one True to evaluate to T 
! turns true into false and false into 
true
]
## 2. Key C# Syntax
a = 5;
b = 10;
int sum = a+b
bool result = (sum > 5)&&(sum <20)
5<2 => false, 5<7 => true 
2>=2 => true, 2>= 4 => false 
4==4 => true, 4==2 => false 
5!=1 => true, 5!=5 => false

## 3. Eureka Exercise/Moment
Assignment 3. i had to read the notes over and  over again to understand and also looked at the one i did.
